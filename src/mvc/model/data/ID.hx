package mvc.model.data;

/**
 * ID Сущности.
 * Целочисленное число, которое не может быть меньше 0.
 * Используется для однозначной идентификации ресурса в реляционной модели данных.
 * @author VolkovRA
 */
typedef ID = UInt;